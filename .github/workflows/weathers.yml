name: wttr.in

on:
  push:
    branches:
    - master
  scheduled:
  - cron:  '0 * * * *'

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        curl wttr.in/Lund
        wget wttr.in/Lund.png
